<% # TODO: sound. %>
<div class="question_prompt">
    <%- @render_concept(@question) %>
</div>
<hr />
<div class="question_type">
    <% if @question_type == 'word_to_image': %>
        <h3>is...</h3>
    <% else if @question_type == 'image_to_word': %>
        <h3>means...</h3>
    <% else if @question_type == 'word_to_word': %>
        <h3>means...</h3>
    <% end %>
</div>

<% # TODO: sound. %>
<div class="possible_answers">
    <div class="ui-grid-a">
    <% chars = 'abcdefghijklmnopqrstuvwxyz' %>
    <% for choice, idx in @choices: %>
        <div class="ui-block-<%- chars.charAt(idx) %>">
            <div class="ui-body">
                <% if choice.get('concept_type') == 'text': %>
                    <a class="answerlink" 
                       data-role="button"
                       data-word="<%- choice.get('concept_value') %>" 
                       href="#"><%- @render_concept(choice) %></a>
                <% else: %>
                    <a class="answerlink" data-word="<%- choice.get('concept_value') %>" href="#"><%- @render_concept(choice) %></a>
                <% end %>
            </div>
        </div>
    <% end %>
    </div>
</div>

<!-- 
TODO: block-a block-b in above stuff
<div class="ui-grid-a ui-responsive">
    <div class="ui-block-a">
        <div class="ui-body">
            <a href="#"><img src="http://placekitten.com/250/250"/></a>
            <span class="hint">C</span>
        </div>
    </div>
    <div class="ui-block-b">
        <div class="ui-body"><img src="http://placekitten.com/250/250"/></div>
            <span class="hint">D</span>
    </div>
</div>
-->
